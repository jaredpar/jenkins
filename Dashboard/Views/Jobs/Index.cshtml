@model Dashboard.Models.AllJobsModel

@{
    ViewBag.Title = "Home Page";
}

@foreach (var name in Model.Names)
{
    <div><a href="@Url.Action(controllerName: "Jobs", actionName: "Name", routeValues: new { name = @name })">@name</a></div>
}

